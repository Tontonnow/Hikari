name: C/C++ CI

on:
  push:
    branches: [ "llvm-21.x" ]
  pull_request:
    branches: [ "llvm-21.x" ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v4
    - name: chmod
      run: chmod 777 ./configure
    - name: configure
      run: ./configure
    - name: make
      run: make
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
